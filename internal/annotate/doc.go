// Package annotate provides rule-based annotation of diff entries,
// attaching metadata labels such as severity and category to help
// consumers prioritize and filter drift results.
package annotate
